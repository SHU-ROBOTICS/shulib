# C2 Progress Log — Motion Scheduler

15:21:50 START C2: motion scheduler. First action per instructions: this progress file. Next: read the brief (C2-motion-scheduler.md), C1-COMPLETED §9/§10, A3-COMPLETED, build-order Phase C, RESUMING, hardware-assumptions, and the motion/control/localization/sim/diag/hal code.
15:22:01 DONE read: C2 brief + C1-COMPLETED (esp. §9/§10 — five named handoffs: fault-reactive cancel policy, activeCommandId assignment, monitor ownership between motions, formalized loop, possible additive ExitReason). Next: A3-COMPLETED, build-order Phase C, RESUMING, hardware-assumptions, then the code.
15:22:15 DONE read: motion.hpp (IMotion/MotionState/MotionDeps), move_to_pose.hpp (full pipeline + exit paths), motion_config.hpp, drive_brake.hpp. Key C2-relevant facts: motions stop motors themselves on exit; post-exit ticks are safe no-ops; watchdog is in-motion; DriveBrake is live-gate exempt (the natural cancel safe-state actor). Next: turn_to/strafe_to/hold_pose/odo_stall_check, control/, localizer surface, sim harness, diag.
15:22:58 DONE read: A3-COMPLETED, RESUMING, build-order Phase C, HA register format (52 entries, C1 added HA-50..52), health_monitor.hpp (Observations; edge-per-episode; brownedOut latched), fault.hpp (FaultCode append-only; MotionTimeout=8), debug_record.hpp (activeCommandId: 'Ids are assigned by the motion scheduler (C2)'). Next: localizer surface, clock/fake clock, term_sink emit contract, loop_monitor, sim scenario/harness, CI guards + CMakeLists.
15:29:00 DONE read: telemetry_sink.hpp (emitRecord idiom + pair rule), scenario.hpp (SimHarness; loop shape controller-first), loop_monitor, clock. DECIDE (forming, will pin after reading RobotContext/TermSink): scheduler = tick-shaped loop owner; blocking verbs tick internally + injected ITickPacer advances the world (host: plant.step; robot: delay) — a pacer that never advances time is DETECTED and precondition-failed rather than hanging. Now reading: robot_context, term_sink, localizer surface, check.hpp, kinematics, CI guards, CMakeLists, C1 routine test.
15:30:03 FOUND check.hpp already assigns C2 a job nobody restated in the brief: the ON-ROBOT precondition policy says the PreconditionError thrown mid-motion is 'caught by the motion scheduler at the task boundary and converted to a FAULT_ABORT exit + a safe drivetrain state'. C2's tick() is that boundary — will implement a tight catch around active->tick() (PreconditionError ONLY), converting to cancel+safe-state+Precondition fault (raised only if the installed handler did not already raise it — faultCount snapshot disambiguates host vs robot policy).
15:30:03 FOUND TermSink renders cmd#id-state numerically — MotionState::Cancelled=5 needs NO TermSink change; the id-stamping decorator is the missing piece for cmd#N.
15:34:46 DECIDE the five load-bearing semantics, after reading everything:
  (1) ONE-AT-A-TIME = single active slot + PRE-EMPT on async-while-active: cancel the old motion (object rendered inert — post-cancel ticks are structural no-ops) THEN swap. Rejected REJECT (a forgotten waitUntilSettled silently skips a move = robot does the wrong thing quietly) and QUEUE (a queue is F2's Sequence in disguise — explicitly fenced out; also hidden latent motions firing later is the scarier failure).
  (2) CANCEL SAFE STATE = 0 V + BrakeMode::Brake applied SYNCHRONOUSLY inside cancel() (one call suffices, no further ticks needed). Rejected coast (robot at speed rolls on into the wall) and closed-loop hold (requires a live, honest estimate — under ODO_STUCK the estimate LIES, and holding against a lie is the exact failure the brief warns about). Brake is estimate-independent, works mid-boot, passive. Efficacy on real hardware = new register entry HA-53 (plant does not model brake modes).
  (3) FAULT POLICY = abort-and-brake on NEW ODO_STUCK raised during the motion (estimate is lying; servoing against a lie is worse than stopping); continue-degraded on IMU_LOST (C1 pinned Degraded does not gate), BROWNOUT (power pathology, estimate still honest; firmware already cut volts; watchdog bounds it), GPS_GATE_REJECT (the gate already protected the estimate), MOTOR_OVER_TEMP + LOOP_OVERRUN (degraded authority/timing, not lies). Configurable abortFaultMask, default = ODO_STUCK bit. NEW-raise detection via additive FaultLatch::raiseCount(code) — a bitmask cannot distinguish a re-raise from a stale latch (found while designing: mask-since-clear would make motion 2 blind to a persisting dead encoder after motion 1 latched it).
15:34:46 DECIDE (cont.):
  (4) NOTHING HANGS: waitUntil takes a REQUIRED explicit finite timeout (rejected a default constant: every bound becomes caller-visible, and no new invented constant to register); entry-check pred BEFORE any tick (true-on-entry = zero ticks); timeout returns WaitResult::TimedOut (distinct enum from Satisfied) + one Warn line, NO fault (a timed-out wait is a legitimate branch, not a pathology). waitUntilSettled needs no own timeout — every motion is watchdog-bounded (C1 mutation #3 proved it). NEW: a pacer that fails to advance the injected clock for 100 consecutive paces is a PRECONDITION failure — an un-catchable hang (watchdogs read the same frozen clock) converted into a loud error.
  (5) RE-ENTRANCY: async()/cancel() from inside a waitUntil predicate are ALLOWED (pre-empt semantics apply; swap happens between ticks so the single-command invariant holds); BLOCKING verbs from inside a predicate are REJECTED by precondition (disguised recursion with user-data-dependent depth); async/cancel from inside a motion tick likewise rejected.
  Plus: the check.hpp task-boundary catch (PreconditionError from active->tick() -> cancel + Precondition fault + run continues) lands here because C2 IS the named 'motion scheduler task boundary'.
15:34:46 START implementation: (a) ExitReason::Cancelled (additive), (b) MotionState::Cancelled=5 + IMotion::cancel() + shared applyCancelSafeState, (c) cancel() in MoveToPose/TurnTo/DriveBrake, (d) FaultLatch::raiseCount (additive, owning A1 file), (e) motion_scheduler.hpp (ITickPacer, WaitResult, CommandIdStampSink, MotionScheduler).
15:41:33 DONE motion_scheduler.hpp (new, ~470 lines): ITickPacer, WaitResult, faultBit, MotionSchedulerConfig (abortFaultMask default=ODO_STUCK), CommandIdStampSink (pair-rule forwarding), CompletedMotion, MotionScheduler (async/tick/waitUntilSettled/waitUntil/cancel + counters + task-boundary catch + stalled-pacer guard). Also: ExitReason::Cancelled appended, MotionState::Cancelled=5, IMotion::cancel() pure virtual + applyCancelSafeState (HA-53) in motion.hpp, cancel() in MoveToPose/TurnTo/DriveBrake, FaultLatch::raiseCount (additive, A1 owning file). Now: quick compile smoke via a scratch TU before writing tests.
15:55:03 DONE tests written: motion_scheduler_test.cpp (25 cases planned), motion_scheduler_routine_test.cpp (4 cases incl. the bit-identity twin — the strongest 'reproduces C1 baseline' statement available), fault_test.cpp +1 (raiseCount owner pins). Building.
15:56:13 FOUND diagnosing 4 test failures: (1) DrivePlant itself emits one TRUTH record per step through the harness sink (A2 design) — those records are UPSTREAM of the C2 stamper and rightly carry id 0/state 0; my id-audit test wrongly assumed only motion records reach the sink. Fix the TEST: discriminate on activeCommandState!=0 (motion records) vs plant/idle records. (2) pre-empt test's second waitUntil pred was true-on-entry (posErr already <25 at swap) so B never drove — pick a pred needing real progress. (3) stale-ODO_STUCK test: freeze at t=3.0 landed after motion 2 settled (or in its low-speed settle phase where spinTravel < threshold) — retime. (4) boot test asserted truth==origin AFTER the pred observed Running, but pred runs between ticks: by then the first live tick has legitimately driven 0.6 in (= one 60 in/s tick). Assert motionlessness DURING the wait from inside the pred instead. All four are TEST bugs, not scheduler bugs — recorded honestly.
15:58:46 DONE suite GREEN: 527 cases / 859,930 assertions (was 487/858,611 — +40 cases, +1,319 asserts), 3 skips unchanged. All four earlier failures were test bugs, fixed as diagnosed. Now capturing MESSAGE numbers (routine accuracy, ODO_STUCK A/B damage, cancel coast distance, settle overhead).
15:59:47 FOUND + fixed a vacuous physics claim: the memoryless rig plant (kA=0) stops INSTANTLY at 0 V, so 'cancel -> drivetrain reaches rest' proved nothing. Re-ran on a LAGGED plant (kA=0.03, tau~0.18 s): cancel from 56.7 in/s coasts 9.7 in to rest in <1.5 s on the zero-volt command alone (real Brake mode only shortens this — HA-53's claim). Added a >1.0 in liveness floor so the test can never quietly go vacuous again. Suite green 527/859,931.
15:59:47 DONE headline numbers through the scheduler: clean sweep IS C1's baseline VERBATIM (bit-identity test passes; n=5 finalErr 0.228175 == C1's 0.228; 40-move worst arrival 0.238); hostile worst 4.13 in (== C1); settle overhead 1.186 s/motion (C1: 1.19). ODO_STUCK policy A/B: abort at 1.51 s / 4.35 in truth-vs-estimate gap vs policy-off twin 6.01 s / 42.0 in — the abort cuts the runaway damage ~10x. Now: CI guards + ARM gate, then the mutation campaign.
16:00:54 DONE guards: GUARD1 PASS (PROS-free incl. scheduler), GUARD2 PASS (core sim-free), ARM cross-compile CLEAN — 86 headers (85+motion_scheduler.hpp; generated list picked it up, no CI edit needed — no new directory). START mutation campaign, 16 planned, each break->build->run->OBSERVE->restore (restores by hand + diff-verify vs pristine copies, NOT git checkout — the C1 process-note lesson).
16:39:09 MUTATE M1 (pre-empt drops the old motion's cancel) -> RED: 2 cases / 16 assertions — the pre-empt case (a.state stayed Running, motors NOT safed at the swap, stray-tick audit) and async-from-pred re-entrancy. Restoring.
16:41:32 MUTATE M2 (applyCancelSafeState drops the 0V command — cancel leaves motors energized) -> RED: 7 cases / 30 assertions: cancel-mid-motion (incl. the never-reaches-rest physics on the lagged plant), pre-empt, cancel-from-pred, ODO_STUCK abort A/B, abort-mask, stale-vs-reraise, task-boundary. THE robot-into-wall bug is comprehensively guarded. Restoring.
16:42:03 MUTATE M3 (safe state Coast instead of Brake) -> RED: 13 cases / 56 assertions — every cancel-path case incl. the panic stop and zero-motion routine. Honest note (in the completion record too): the plant does not model brake modes, so this is state-inspection detection (HA-53 owns the physics). Restoring.
16:42:52 MUTATE M4 (fault policy dead: newlyRaisedAbortFault always None) -> RED: 3 cases / 3 fatal REQUIREs — ODO_STUCK A/B (rode to 6 s watchdog instead of the 1.5 s abort), re-raise case, configurable-mask case. Restoring.
16:48:00 MUTATE M5 (waitUntil deadline check dropped) -> RED: 5 cases / 6 assertions, via the CAPPED pacer (throw) — never-true, poll, wait-during-motion, boot-cancel, idle-gap. The suite went red in bounded time, NOT into a hang (the bounded-loop shape doing its job). Restoring.
16:48:28 MUTATE M6 (waitUntil timeout reported as Satisfied) -> RED: 5 cases / 5 assertions (distinguishability pins). Restored.
16:51:04 MUTATE M7 (waitUntil entry-check dropped: pred evaluated only after a tick) -> RED: 1 case / 3 assertions — the true-on-entry/poll case, its designed detector (specific by design, like C1's #2: elsewhere one extra tick is slower, not wrong). Restored.
16:52:40 MUTATE M8 (id stamp dropped: decorator forwards unstamped) -> RED: 3 cases / 260 assertions — both ids cases + the cancel-record id pin. Restored.
16:54:25 MUTATE M9 (pair rule broken: stamper wantsRecord()=true unconditionally) -> RED: 1 case / 1 assertion — the records-off probe saw emit() fire (the A1 cost-contract detector, exactly as designed). Restored.
16:55:49 MUTATE M10 (idle HealthMonitor tick dropped — the monitor gap re-opens) -> RED: 1 case / 1 assertion — the idle-gap IMU_LOST case, its designed detector. Restored.
16:56:14 MUTATE M11 (localizer.update() dropped from the scheduler tick) -> RED: 29 cases / 38 assertions — essentially every C2 case (motions servo a frozen estimate: timeouts, misses, aborts everywhere). The formalized loop's estimate-advance step is maximally load-bearing. Restored.
17:00:00 MUTATE M12 (loop shape broken: motion ticks BEFORE the estimate advances — one-tick-stale controller) -> RED: 2 cases / 40 assertions — ONLY the two bit-identity equivalence cases caught it (everything else converges on a one-tick-stale estimate; slower, not wrong). This is exactly why the equivalence tests exist: they are the sole detectors of loop-shape drift, the C2 analogue of C1's mutation #2 being visible only to the simultaneity pin. Restored.
17:03:38 MUTATE M13 (stalled-pace guard removed) -> RED: 1 case / 2 assertions — the FrozenPacer case went to its own cap (runtime_error instead of PreconditionError) — red in bounded time, not a hung suite. Restored.
17:05:26 MUTATE M14 (task-boundary catch removed — a mid-tick PreconditionError escapes) -> RED: 2 cases / 21 assertions — both task-boundary cases (the exception propagated out; motors would have stayed at 5V). Restored.
17:05:50 MUTATE M15 (cancel verdict-preservation removed in MoveToPose — cancel rewrites Settled/TimedOut to Cancelled) -> RED: 2 cases / 5 assertions — cancel-after-exit and cancel-already-timed-out. Restored.
17:08:06 MUTATE M16 (fault-count snapshot zero-filled: stale faults look freshly raised) -> RED: 1 case / 1 assertion — the stale-vs-reraise case (motion 1 insta-aborted on the pre-existing latch entry). Restored. CAMPAIGN COMPLETE: 16/16 mutations RED, zero GREEN survivors — every load-bearing decision has a designed detector.
17:08:15 DONE post-campaign integrity: all 7 touched headers byte-identical to pristine (cmp-verified), no mutation markers in include/ (grep — the one hit is A2's pre-existing truth_integrator doc comment), suite re-green 527/859,931. Next: documentation contract — HA-53 register entry, roadmap checkbox + you-are-here, C2-COMPLETED.md.
17:09:12 DONE HA-53 registered (52 -> 53 entries; index + full entry + HA-52 blast-radius amended for the C2 abort policy; code anchor PROVISIONAL (A4: HA-53) in motion.hpp; reconciliation grep both directions clean). Next: roadmap + build-order position updates.
17:16:35 DONE documentation contract: C2-COMPLETED.md written (13 sections, incl. 'What we now know for certain, and what we do not' + the F6 flag list + the fault-policy A/B table); roadmap checkbox flipped with cited evidence + 'you are here' updated (Next: C3); build-order Current position updated (C1 stale pending-review note also corrected — it is committed); HA register 52->53. Final: full independent re-verification pass.
17:17:54 DONE final verification, all as observed: CLEAN rebuild green 527 cases / 859,931 assertions / 3 skips; GUARD 1 PASS; GUARD 2 PASS; ARM cross-compile CLEAN (86 headers); nothing committed (HEAD unchanged at e52b190; 16 working-tree entries for review). C2 complete: code + 40 tests + 16/16 mutations red + HA-53 + roadmap/build-order/COMPLETED docs. Handing back for independent verification.
