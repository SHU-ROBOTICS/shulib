# C1 Progress Log — Motion Primitives

11:05:57 START — C1 chunk opened. First action per protocol: this progress file. Next: required reading (brief, A3/A2 COMPLETED, build-order, RESUMING, hardware-assumptions, then all relevant code).
11:06:10 DONE — read C1 brief, A3-COMPLETED, A2-COMPLETED, RESUMING. Key handoffs confirmed: (1) wait-for-live-estimate contract, (2) ODO_STUCK spin-vs-motion cross-check (HealthMonitor::odomStalled shape already exists per A3 D3). Next: build-order Phase C + hardware-assumptions register.
11:07:18 DONE — read control/ (Pid, ExitGroup, SettledUtil, Watchdog, FF, TrapezoidProfile), frame.hpp, kinematics contract, Localizer (boot guard + qualityClass), HealthMonitor (odomStalled observable already shaped for C1), DebugRecord (C1 owns most fields), telemetry seam, plant + harness + survival matrix. FOUND — survival ATTACK 4 test already prototypes the exact spin-vs-motion cross-check shape C1 must own as a library class.
11:14:23 START — ESCALATION received from user via coordinator: test bar raised well above the brief (settled-vs-truth divergence bounding, full seeded sweeps, universal invariants, rotation/mirror equivariance, ±180 seam, adversarial geometry, composition, scaling sanity, mutations beyond four, named-bug-per-test). Treating brief's list as FLOOR. Planning test architecture around it before writing code.
11:17:10 DECIDE — new dir include/shulib/motion/: motion.hpp (IMotion + MotionState wire vocabulary + MotionDeps), odo_stall_check.hpp, move_to_pose.hpp (the 3-axis engine), turn_to.hpp, strafe_to.hpp, hold_pose.hpp, drive_brake.hpp. One class per header, matching control/ style.
11:17:10 DECIDE — IMotion is TICK-shaped (start()/tick()→ExitReason), not loop-owning: the LOOP updates the Localizer then calls tick(); C2's scheduler owns the real loop. Same controller-first shape A2 documented in scenario.hpp.
11:17:10 DECIDE — wait-for-live-estimate contract = WAIT-BOUNDED-BY-WATCHDOG: while qualityClass()==Uninitialized a motion commands ZERO volts and makes no settle progress; the watchdog (armed at start()) keeps running, so a never-live estimate exits TimedOut + MOTION_TIMEOUT fault with zero voltage ever commanded. Rejected: refuse-at-start (an auton issued during calibration would just die — real autons wait out calibration, per the A3 survival test's own loop) and fault-immediately (boot is NORMAL, not a fault — HealthMonitor's own rule). Degraded does NOT gate: a mid-run dropout keeps driving on the stale-heading estimate (freezing mid-run strands the robot; Localizer D8 chose the same).
11:17:10 DECIDE — capture-at-live-tick rule: StrafeTo's held heading and HoldPose's captured pose are read at the FIRST LIVE tick, not at start() — capturing during Uninitialized would target calibration garbage. Part of the wait contract, tested.
11:17:10 DECIDE — DriveBrake is EXEMPT from the live gate: zero-output is the safe boot action and must never be gated behind an estimate. Its settle verdict reads the estimate (best available; documented).
11:17:10 DECIDE — ODO_STUCK cross-check = OdoStallCheck class owned by every motion (not optional): windowed mean |drive-shaft delta|·r vs observed motion hypot(dx,dy)+R·|dtheta|; stalled when spin>=minSpinTravel AND observed<ratio·spin. Rotation term makes TurnTo immune to false positives AND a frozen tracking encoder does not false-fault TurnTo (heading is IMU-owned — position odometry being dead cannot hurt a pure turn). Slip margin: 70% propulsion ratio >> 0.25 threshold.
11:17:10 DECIDE — every motion ticks HealthMonitor itself (imuReady, odomStalled, odomImplausible via a new additive Localizer accessor, fixGated, batteryVolts, maxMotorTempC): the active motion IS the loop at C1, so the A3 containment wiring lives in the library, not in each test. Additive Localizer::lastOdomDeltaImplausible() forwarder added (C1 consumer need; PilonsOdometry stays policy-free).
11:17:10 DECIDE — pipeline order (each step one layer, frames explicit): field errors → per-axis PIDs (x,y,theta DECOUPLED; ONE shared translation gain set on x AND y — unequal gains would break rotational equivariance of field behaviour) → norm-cap linear + clamp omega (direction-preserving; per-axis caps would curve diagonals) → fieldToRobot (THE one F1 call) → strafe-authority clamp on BODY vy (|vy| <= authority·maxLinearSpeed — F5: this layer clamps, kinematics never) → toWheels → desaturate → FF → compensateForBattery → setVoltage.
11:17:10 DECIDE — heading PID encoding pid.update(0, -errH) with errH = pose.heading().errorTo(target): wrap absorbed BEFORE the PID via F3's shortest-error, D-on-measurement stays live. Raw theta differences never reach a controller.
11:17:10 DECIDE — MoveToPose composes two SettledUtils (translation norm, heading) + one Watchdog instead of ExitGroup (which is 1 scalar + watchdog): same tested primitives, 2+1 composition, Settled-beats-TimedOut priority preserved. TurnTo and DriveBrake use ExitGroup unmodified.
11:17:10 DECIDE — HoldPose verdict: runs for holdFor, final verdict Settled iff currently within tolerances else TimedOut — "held the clock out while 10 in off" must not report success.
11:17:10 DECIDE — DebugRecord.commanded = the FINAL achievable command expressed in FIELD frame (robotToField of the post-clamp body command, built lazily in the record builder): keeps the schema's field-frame section honest AND makes the strafe/authority clamp mutation-observable from records.
11:17:22 START — second user requirement received: FULL-ROUTINE accuracy test (10-20 waypoint chains under hostility, error vs move COUNT / total DISTANCE / elapsed TIME regressed separately to attribute residuals to C1 vs register vs Phase E; bounded end error that does NOT trend with move count because targets are ABSOLUTE field poses; directional-bias check; clean-baseline twin). Hand-chained here (no scheduler needed); anything genuinely C2-shaped will be named. Now writing include/shulib/motion/ headers.
11:25:17 DONE — include/shulib/motion/ written (motion.hpp IMotion+MotionState+MotionDeps, motion_config.hpp, odo_stall_check.hpp, move_to_pose.hpp engine, turn_to.hpp, strafe_to.hpp, hold_pose.hpp, drive_brake.hpp) + additive Localizer::lastOdomDeltaImplausible() + both CI guards extended with include/shulib/motion. Host strict-flags smoke compile CLEAN, ARM cross-compile smoke CLEAN (85 headers now). Next: the test suite — starting with core primitive behaviour.
11:34:25 FOUND — TermSink rendered an ACTIVE C1 motion as '[LOC] idle': it discriminated the [MOT] channel on activeCommandId alone, but ids are C2-scheduler-assigned — a standalone C1 motion has id 0, state != 0. First-consumer defect, fixed in the owning A1 file (state clause added); all existing golden-line pins unaffected (a genuinely idle record has both zero).
11:34:25 FOUND — HoldPose fighting a sustained shove reaches a wheels-spinning/robot-stationary equilibrium == the stall signature, so ODO_STUCK fires before MOTION_TIMEOUT. By design (same fault family as a blocked drivetrain); pinned in the test with the reasoning.
11:38:36 DONE — motion_primitives_test.cpp (20 cases), motion_frames_test.cpp (7: heading sweep, rotation+mirror equivariance @0.02in, ±180 seam incl. F3 antipodal-CCW pin), motion_sweep_test.cpp (5: 24 X-drive + 12 tank seeded trials, per-tick universal invariants, saturation-binds proof, abandon-and-retarget). C1 subset: 30 cases / 141,193 assertions green in 61 ms. Next: hostile survival + wait-for-live + ODO_STUCK + settled-vs-truth divergence measurement.
11:43:29 FOUND — under composed hostility the Localizer twist (raw fused-position finite difference) has a ~0.5–1.5 in/s noise floor AT A PHYSICAL DEAD STOP (heading noise sigma 0.05 deg through the offset-correction path, differentiated at 100 Hz). DriveBrake's 0.5 in/s settle threshold sat BELOW the estimator's own noise floor — it could never certify 'stopped' under hostility. Also: during the boot window the yaw-rate stream is calibration garbage (+/-10 rad/s), so a boot-time brake's VERDICT defers ~2 s even though its zero-volt ACTION is immediate.
11:43:29 DECIDE — fix in DriveBrake, not the Localizer: 5-tick vector average of (vx,vy,omega) before the norm (sqrt-n noise reduction, 50 ms verdict delay), brakeSettle default raised to 1.2 in/s / 100 in/s^2 (above the averaged floor, registered HA-51). Rejected: smoothing inside Localizer (its raw finite-difference is documented M2 semantics Phase E measures against); rejected: tolerating a never-settling brake.
11:46:32 DONE — motion_hostile_test.cpp (13 cases): wait-for-live (waits at ZERO volts through calibration, never-live exits TimedOut having never moved, StrafeTo captures POST-boot heading, DriveBrake exempt: action immediate/verdict deferred), ODO_STUCK frozen-encoder under the A3 hostile model (estimator blind CONFIRMED, cross-check catches it; pure TurnTo immune), per-family + composed settled-vs-truth divergence matrix, sentinel breach via odomImplausible wiring, mid-run IMU dropout does NOT gate (Degraded keeps driving), brownout-first-then-timeout, thermal MOTOR_OVER_TEMP through the motion's own wiring, jitter+full-hostility convergence. Full suite 472/856,596 GREEN. Next: OdoStallCheck unit tests + the full-routine accuracy test.
11:57:03 DONE — motion_stall_check_test.cpp (9 unit cases incl. the ±180-seam masking case and the 70%-slip margin) + motion_routine_test.cpp (4 cases, the full-routine accuracy escalation). Suite 485 cases / 857,566 assertions GREEN.
11:57:03 FOUND — routine numbers (the headline): CLEAN plant error is FLAT in move count — 5/10/20/40-move routines all end 0.00–0.24 in with worst arrival 0.24 in (count-compounding structurally absent; absolute targeting works). HOSTILE (full composed A3 world): worst arrival 4.13 in / final 3.15 in on the worst boot (seed 11, 20–40 moves, ~95 s, ~900 in); typical boots 0.6–2.6 in. Slopes: 0.062 in/move raw, 0.028 in/s vs time, 0.003 in/in vs distance — the time regressor carries it (M2 drift, Phase E's problem). A/B discriminator: 16x15in vs 8x30in = 1.22 vs 0.73 in (no per-move compounding; B pays only its extra 12 s of drift). Miscalibration twin: 2% fat wheels regress err-vs-displacement at 0.0194 (the injected 2%, recovered); calibrated slope -4e-5. Stop-and-settle overhead ~1.17 s/motion at provisional gains — the number that informs the Frontier blending decision.
11:57:03 FOUND — first cadence draft had every sweep length ENDING on a strafe leg, so final heading error was never truly graded (read 0 by construction). Fixed: strafe cadence k%7==3 (no length ends on one) + worst-heading-arrival tracked; hostile worst heading 1.19 deg.
11:57:03 START — mutation campaign. Plan: (1) swap fieldToRobot->robotToField, (2) sequence turn-then-drive, (3) defeat watchdog, (4) drop stall cross-check, (5) drop strafe-authority clamp, (6) drop wait-for-live gate, (7) raw heading difference instead of errorTo, (8) drop desaturate, (9) drop stall check's rotation term, (10) exit on translation-settled alone. Each: break -> build -> run -> observe -> restore -> re-green.
11:57:51 MUTATE #1 fieldToRobot->robotToField in the pipeline -> RED: 15 cases / 15 fatal assertions (frames heading-sweep + BOTH equivariance cases + primitives from-varied-starts + decoupled proof + composition + routine chains + hostile divergence + jitter + sweep abandon-retarget; even the M2 acceptance case). Restored, suite re-green 485/857,566.
11:58:10 (correction to the #1 entry: the M2 acceptance case was NOT among the reds — a doctest flag misread on my side; the 15 red cases are the C1 frames/primitives/hostile/sweep/routine cases funneling through the 10 REQUIRE sites listed in the run output. Recording as observed.)
12:03:37 MUTATE #2 turn-then-drive sequencing (zero translation while |errH|>0.05) -> RED: exactly 1 case — 'C1 decoupled: diagonal move + 90deg heading change happen as ONE motion' (transFracWhenHeadingArrived read ~0 vs required >0.30). The holonomic-thesis detector works and is SPECIFIC: everything else still converges (sequencing is slower, not wrong — only the simultaneity pin can see it). Restored.
12:06:46 MUTATE #3 watchdog defeated in MoveToPose (both expiry checks short-circuited) -> RED: 7 cases / 15 assertions — unreachable-target, never-live wait, tank StrafeTo-impossible, zero-tolerance, frozen-encoder containment, brownout, thermal. Every bounded-loop test observed the motion still Running at maxTicks instead of hanging forever (the bounded-loop test shape means a defeated watchdog reads as red, not as a hung suite). Restored.
12:07:04 MUTATE #4 ODO_STUCK cross-check dropped (stall verdict forced false in MoveToPose) -> RED: 3 cases / 3 assertions — the frozen-encoder A3-model case (firstFault no longer OdoStuck), the unreachable-target case, the HoldPose push-equilibrium case. The only defence against a dead encoder is proven load-bearing. Restored.
12:07:34 MUTATE #5 strafe-authority clamp dropped -> stayed GREEN. A HOLE IN THE SUITE, found exactly as the escalation predicted: the clamp is structurally unobservable on tank (toWheels discards vy regardless) and vacuous on X-drive (authority 1.0 makes the limit == the norm cap). The clamp's only real consumer is a FRACTIONAL-authority drive — C3's H-drive. Closing the hole with a pseudo-H test drive (X geometry, declared authority 0.35): a lateral move must RIDE the 0.35*maxLin body-vy limit and never exceed it, audited from records. Also auditing the tank StrafeTo attempt's records (off-axis demand -> clamp zeroes body vy).
12:12:28 MUTATE #5 re-run against the strengthened suite -> RED: 2 cases / 150 assertions — the new fractional-authority (0.35) drive audit (body vy blew through the 21 in/s limit in the records) and the tank StrafeTo record audit (lateral demand leaked into body vy). Hole CLOSED with the exact contract shape C3's H-drive consumes. Suite baseline is now 486 cases / 857,945. Restored.
12:12:45 MUTATE #6 wait-for-live gate dropped (live forced true) -> RED: 9 cases / 29 assertions — all three wait-for-live pins (drove during calibration on the frozen boot pose; the never-live case 'settled' at the phantom origin; StrafeTo locked garbage heading) PLUS the composed/divergence/jitter/sentinel/routine cases (driving on a not-yet-live estimate mis-lands under hostility). Restored.
12:13:35 MUTATE #7 raw heading subtraction instead of F3 errorTo (MoveToPose + TurnTo) -> RED: 2 cases / 6 assertions — the ±180 seam case observed LONG-WAY rotation on every crossing (deltaDeg −320-class instead of +40-class), and the hostile routine sweep's worst-heading bound blew. Restored.
12:13:47 MUTATE #8 desaturate() dropped from the MoveToPose pipeline -> RED: 2 cases / 2 fatal assertions — the saturation case and the 24-trial invariant sweep both tripped the FF-implied-wheel-speed budget REQUIRE (wheels demanded past maxWheelSpeed reached the motors). Restored.
12:14:26 MUTATE #9 stall check's rotation term dropped -> RED: 1 case / 90 assertions (the pure-rotation unit case: every window falsely read stalled). Noted while analyzing: the INTEGRATED frozen-encoder TurnTo case survives this mutation because frozen tracking wheels during rotation produce PHANTOM fused translation (= |offset|*dTheta through the Pilons offset correction — known A3 physics), which masks the stall there; the unit case is the designed detector precisely because it controls its observables exactly. Restored.
12:15:38 MUTATE #10 heading settle criterion dropped (exit on translation alone) -> RED: 3 cases / 4 assertions — the 24-trial sweep's heading-arrival REQUIRE and both routine sweeps (waypoints 'settled' with the heading still turning; worst heading arrival blew). Restored.
12:15:59 MUTATE #11 compensateForBattery dropped -> stayed GREEN. SECOND HOLE: on a nominal pack (12.6 V) the compensation never binds (motion demands cap at ~11.4 V) and the brownout case is masked by the hostile model zeroing EFFECTIVE volts regardless of the command. Closing with a low-pack (8 V) run asserting every commanded voltage respects the battery ceiling while the motion still completes.
12:18:20 MUTATE #11 re-run -> RED: 1 case / 1 fatal assertion — the weak-pack ceiling REQUIRE tripped at 11.4 V commanded vs the 8 V battery. Hole closed; baseline now 487 cases / 858,611.
12:19:45 MUTATE #12 TermSink [MOT]-discriminator fix reverted -> RED: 1 case / 1 assertion (the legibility case saw '[LOC] idle' for an active motion again). The first-consumer fix is pinned. Restored (and re-applied after an over-eager git checkout also reverted the legitimate fix — caught, re-verified).
12:22:17 DONE — mutation campaign complete: 12 mutations executed, 12 observed RED after two suite-strengthening rounds (#5 authority clamp and #11 battery compensation were GREEN on first run — both holes closed with new tests, re-run RED). Final baseline 487 cases / 858,611 assertions. Both CI guards PASS locally (motion/ in scope), ARM cross-compile CLEAN at 85 headers, motion headers verified byte-identical to pre-mutation pristine copies. Next: documentation (register HA-50..52, roadmap, build-order, C1-COMPLETED.md).
12:35:30 DONE — register (HA-50/51/52, counts 52, reconciliation bidirectional), roadmap (WS6 + WS13 exit-reason clauses flipped with evidence, you-are-here -> C1 done/C2 next, status paragraph -> 487/858,611 + 85 ARM headers), build-order (current position + verified block + named C2 handoffs). Next: C1-COMPLETED.md.
