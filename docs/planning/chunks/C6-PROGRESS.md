# C6 Progress — legacy salvage audit

17:04:49 START C6: last-look audit of legacy/ before C7 deletion. Reading brief + required docs first.
17:05:11 DONE read brief C6-legacy-salvage.md; enumerated legacy tree: 18 include + 16 src = 34 files exactly, matches brief
17:05:11 START required reading: A1-COMPLETED, roadmap M2, master-plan §16.3/§14, build-order, RESUMING, hardware-assumptions
17:05:51 DONE required reading pt1: brief, A1-COMPLETED (D4 clean-room logger rejection + §6 sendDebugMessages worse-than-dead finding), roadmap M0 quarantine + M2 cutover block, master-plan §16.3/§14, build-order C6/C7/G2/G4, RESUMING
17:05:51 FOUND roadmap M2 'Salvage before deleting' checkbox still says 'port RobotCommands→sequence/ seed, logger.hpp→io/Telemetry' — that wording predates A1's clean-room supersession; C6 must reconcile it
17:05:51 START reading hardware-assumptions.md, then ALL 34 legacy files
17:06:08 DONE hardware-assumptions.md skim: 57 HA entries, index read; legacy constants found in audit will be checked against it
17:06:08 START reading all 34 legacy files (sizes first)
17:06:42 FOUND legacy/pose.hpp line 1: '// from lemlib' + GitHub URL — legacy contains COPIED LemLib code (provenance fact; strengthens both clean-room and deletion rationale)
17:06:42 FOUND RobotCommands defines the SAME 6-command enum TWICE (C autonomous_commands.h typedef enum vs C++ CommandType.hpp) and the same payload struct twice (C 'Command' struct vs 'CommandStruct'); worse, Command.hpp's OO 'Command' class name-collides with the extern-C 'Command' struct — an ODR hazard baked into the design
17:06:42 FOUND MoveWithHeadingCommand::execute() calls a private moveTo() whose body is EMPTY — the only concrete OO command is unimplemented scaffolding
17:06:42 FOUND the 6-command vocabulary (headers): MOVE_WITH_HEADING, PICK_UP, PLACE, SCOOP, RELEASE, CLASP — awaiting CSV cross-check
17:07:30 FOUND odometry.cpp:338-355 — ROADMAP CLAIM 1 VERIFIED IN SOURCE: 'odomPose.theta += localPose.theta' runs BEFORE the chord rotation, which then uses cos/sin(odomPose.theta) = the NEW heading, not the tick-average. Exactly the bug the clean-room arcStep re-derivation caught. Ironic detail: estimatePose() (line 119) uses avgHeading = theta + delta/2 CORRECTLY — the prediction path did what the integration path didn't
17:07:30 FOUND odometry.cpp hard-won numbers: legacy robot's REAL odom geometry in the stuck-detection log strings — LEFT rotation port 8 (comment says -8 = reversed), offset -6.5in; RIGHT port 10, offset +6.5in; BACK port 9, offset +2.5in. HA-12's stand-ins are -3.0/-4.5; the last real robot measured ±6.5 L/R + 2.5 back. R-phase provenance input
17:07:30 FOUND odometry.cpp:323 vs 332 — the (sL==sR) divide-by-zero guard exists ONLY on the logging path; the actual heading math one page later divides unguarded. A latent config-error crash the authors half-knew about
17:07:30 FOUND odometry.cpp: the IMU is plumbed into OdomSensors but NEVER read in update() — legacy heading was wheels-only, with thetaCorrectionFactor as the scale-error band-aid. v2's IMU-owned heading is the structural fix
17:07:30 FOUND odometry.cpp wheel-health monitor (stuck 500ms zero-while-moving, intermittent <50% travel ratio, L/R imbalance >10%) — genuine operational knowledge (they lived this failure: essay-length checklists name physical causes). Candidate requirements input for diagnostics backlog — check diagnostics-plan.md for coverage
17:07:30 FOUND odometry.cpp:339 hazard: plain abs() on float with <math.h> — resolves to int abs on some libcs (silent truncation); implementation-defined landmine, another clean-room justification
17:08:06 FOUND drivetrain.cpp:13-14 field-centric rotation bug: 'horizontal' is overwritten then reused to compute 'vertical' — the rotation matrix is applied with a corrupted input. Every legacy field-centric drive was subtly wrong (plus int truncation of the trig products)
17:08:06 FOUND util.cpp:20-21 angleError bug: sanitizeAngle(target) called TWICE, position never sanitized — copy-paste defect; CW/CCW-forced turns computed error from an unbounded position
17:08:06 FOUND util.hpp/cpp landmine: sanitizeAngle declared constexpr in the header but DEFINED only in the .cpp — ill-formed NDR for any other TU that calls it
17:08:06 FOUND chassis.cpp: (a) Chassis stores Drivetrain BY VALUE — every TankDrive/XDrive passed in is object-sliced; (b) line 69 copy-paste: right tracking wheel nullptr throws 'Left tracking wheel not initialized'; (c) moveToLocalPose is dead scaffolding (unused int-truncated 'rotations', open-loop move_relative, async param ignored, wait loops commented out) — legacy NEVER had closed-loop motion
17:08:06 FOUND odomUnit.cpp:17 — the 36000 ticks/rev (centidegree) Rotation conversion: HA-16's 'measured elsewhere' is HERE; legacy is the provenance
17:08:06 FOUND pid.cpp: no integral clamp (windup), derivative-on-error, unguarded divide-by-time — superseded by v2 control/Pid (M2 WS4)
17:08:06 START logger.cpp, main.cpp, autonomous_commands.c, CSV, GUI files
17:08:36 DONE logger.cpp+hpp — ROADMAP CLAIM 2 VERIFIED MECHANICALLY: (1) escapeJSONString defined hpp:141, ZERO call sites (grep) — messages enter JSON unescaped at cpp:36; (2) sendDebugMessages declared hpp:172 + called cpp:17, never defined — the TU cannot link; (3) init() spawns a 100ms background sendTelemetry task while update() also calls it manually = the race, plus pros::delay(5) held UNDER the mutex. All three match A1-COMPLETED D4's designed-out defects; nothing here needs a port (JSON wire role = H1 SHUL/2, planned)
17:08:36 FOUND logger.cpp maxSize=900-byte chunking + inter-chunk delays — archaeological evidence the team hit V5 serial backpressure; corroborates §18's rate-budget requirement and E1 SdSink back-pressure design. Not a port, a confirmation
17:08:36 START main.cpp (1250 lines)
17:09:36 DONE main.cpp (1250 lines). FOUND hard-won constants: full port map of the last real robot ('pookster' tank): L drive {11,-12,13,-14,-15}, R drive {16,-17,18,-19,20} (5-motor sides), rotation L(-8)/R(10)/B(9), IMU port 6, ADI arm 'B' lever 'C', intake {2,-3}, conveyor {4,-5}, releaser {-6,7}; odom wheels 2.75in dia at offsets -6.5/+6.5/+2.5; TankDrive(trackWidth 15, wheel 3.25, rpm 400); start pose (36,-60,-180deg)
17:09:36 FOUND main.cpp:838-839 measured minimums (via test_min_output ramp): MIN_OUTPUT_Y=20, MIN_OUTPUT_THETA=25 (of 127) — an empirical kS measurement (~1.9V linear); HA-45's kS placeholder is 1.0V. R5 input + the test_min_output ramp is a calibration-routine specimen for M3
17:09:36 FOUND main.cpp defects: (a) move_to_pose computes rotationOutput then NEVER applies it (drive(0,fwd,0)) — heading-hold silently dead; (b) rotate_to wraps error at ±181 not ±180; (c) PID dt passed as 5 (seconds!) for linear vs 0.005 for heading in the same loop; (d) the entire competition routine is COMMENTED OUT — live autonomous() is brake-hold + rotate_to(90)
17:09:36 FOUND main.cpp practice: setPose(0,0,theta) re-zeroed after EVERY turn — they re-anchored odometry constantly because wheel-only heading drifted; the mechanism verbs actually used: lever.extend/retract, intake/conveyor/releaser combos (limitedCombo/limitedComboFull), oscillation (wiggle-to-seat) — F2/G2 vocabulary evidence
17:09:36 FOUND main.cpp rotation_calibration(): iterative IMU-vs-wheel-odom theta-scale calibration; test_min_output(): +1 power per 500ms ramp — both are R-phase calibration-routine specimens
17:09:36 START autonomous_commands.c + CSV mechanical extraction
17:12:01 DONE mechanical extraction (scripted, all 4 sources). AUTHORED vocabulary = {MOVE_WITH_HEADING x826 CSV + 635 .c + 10 main.cpp; NONE x18 CSV + 1 main.cpp}. DECLARED-only: PICK_UP/PLACE/SCOOP/RELEASE/CLASP appear in the two enums and NOWHERE in any data — mechanism intent, never authored
17:12:01 FOUND NONE is in the DATA but NOT in either enum — the exporter emitted a command id the C importer literally cannot represent. All 18 NONE rows have speed=0 and sit at segment starts (+file end) = path-boundary markers. G4 importer requirement + G2 requirement (explicit waypoint-only/no-op id)
17:12:01 FOUND unit instability across specimens: CSV fits the 144in field (x 3.7-123, y 14-119, heading signed deg, speed const 50); the .c does NOT (y up to 343 — cm or canvas px?) and carries per-waypoint speed RAMPS 41.8-100. G4 importer CANNOT assume inches; needs unit detection/metadata. Waypoints are ~1in-spaced dense interpolation, commands interleaved as ROWS not markers — G4 must convert rows to G2 markers
17:12:01 START GUI remnants (gui.cpp.ignore, shulcd.c.ignore, GUI/gui.cpp.txt, GUI/gui.c.txt, setonhalllogo.c)
17:12:24 DONE GUI remnants: llemu-clone scaffolding (shulcd.c.ignore), two non-compiling .txt drafts (gui.cpp.txt refs 'ui->' with no ui in scope), an LVGL bitmap of the Seton Hall logo (asset, regenerable), plus gui.hpp/gui.cpp.ignore stubs. Discard-class; one salvageable IDEA: alliance-color screen + boot logo + on-screen log = R-phase/VexBuilder UX territory, noted in the audit
17:12:24 START v2-tree cross-reference for 'superseded by' targets
17:14:31 FOUND (decisive): NO EXECUTOR EVER EXISTED. main.cpp's autonomousCommands vector is defined and never read; the .c array is extern-declared in two headers with no consumer; Chassis::commands is never populated. The team built THREE parallel data representations of routines and never got one to run — the autons that ran are the hand-written blocking calls. This is G2's reason-to-exist, documented in their own tree
17:14:31 DONE all 34 files read. START writing docs/planning/legacy-command-vocabulary.md
17:16:06 DONE legacy-command-vocabulary.md (7 ids mapped, 2 named gaps, 7 G4 importer requirements, no-executor finding, method appendix)
17:16:06 START hardware-assumptions.md addendum: legacy-measured reference constants (scoped as evidence, NOT as new HA values — different robot)
17:18:11 DONE diagnostics-plan.md C6 note (per-wheel stuck attribution → E-phase detector; veer triage → H2/R-phase; 900-byte serial evidence)
17:18:11 START C6-COMPLETED.md — the 34-file table + claims verification + verdict
17:21:22 DONE C6-COMPLETED.md; verification RE-RUN for real: suite 659/915570/3 skips, guard1 PROS-free PASS, guard2 sim-layering PASS, ARM gate 102 headers CLEAN
17:21:22 START roadmap + build-order status updates
17:23:08 DONE roadmap salvage checkbox flipped [x] with per-item evidence; 'you are here' moved to C6-done/C7-next in both roadmap and build-order; stale C5 'pending commit' lines corrected to committed be6d129
17:23:08 DONE segment-count correction: 18 NONE markers delimit 17 move segments (sum-checked: 62+44+...+115=826), fixed in both vocabulary tables
17:23:08 DONE final verification after ALL edits: suite 659 cases / 915,570 assertions / 3 deliberate skips GREEN; PROS-free guard PASS; sim-layering guard PASS; ARM gate 102 headers CLEAN; git status = 4 modified planning docs + 3 new docs, NOTHING committed
17:23:08 DECIDE port list EMPTY (D1, justified in C6-COMPLETED §3): 11 live legacy bugs catalogued, no v2 consumer for any candidate, requirements lodged with owners instead
17:23:08 DONE C6 closes: all 34 files classified, 3 claims verified in source, vocabulary mechanically cross-checked, safe-to-delete verdict UNCONDITIONAL (C6-COMPLETED §8). C7 may rm -rf.
