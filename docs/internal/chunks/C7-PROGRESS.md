# C7 Progress — legacy cutover, README, docs reorg

17:28:36 START C7. Reading brief (C7-cutover.md), C6-COMPLETED.md verdict, and required docs before touching anything.
17:29:17 DONE reading C7 brief + C6-COMPLETED verdict (§8 unconditional safe-to-delete; 34/34 classified). Proceeding to required reading: README, Makefile, common.mk, ci.yml, build-order, roadmap, RESUMING.
17:29:48 DONE required reading pass 1 (brief, C6 verdict, README, Makefile, ci.yml, RESUMING, build-order C7, roadmap M2 block). FOUND: src/ contains ONLY legacy/ — there is no src/main.cpp at all; C7 must create it fresh. include/main.h + api.h are PROS template files.
17:31:15 DONE surveying build system. FOUND: common.mk compiles ALL of src/** via rwildcard; EXCLUDE_SRCDIRS is a supported seam — 'make EXCLUDE_SRCDIRS=./src/legacy' lets me prove the new main.cpp builds WITH legacy still present, no Makefile hack needed. FOUND: include/main.h still includes legacy 'shulib/api.hpp' (broken) — must be rewritten too. firmware/ = libc/libm/libpros + linker scripts (no liblvgl.a).
17:34:38 DECIDE Coordinator refinement received: docs/internal/ must be a removable unit — nothing outside it may link into it; public docs must stand alone; prove with a move-aside link check. Scoping the reference graph now (docs/planning mentions across code/tests/docs).
17:37:36 START Phase 1 — rewiring: new include/main.h (drops legacy shulib/api.hpp umbrella + namespace pollution) and new src/main.cpp (full v2 object graph, fake-backed HAL seams marked TODO(R1), TermSink on USB stdout). Precondition policy stays default-throwing; R1's bootstrap owns the robot policy handler.
17:39:38 START Phase 1 build: make clean && make EXCLUDE_SRCDIRS=./src/legacy — legacy still on disk, excluded via common.mk's own seam, so the new main.cpp/main.h are the only variables.
17:39:54 DONE Phase 1: make clean && make EXCLUDE_SRCDIRS=./src/legacy SUCCEEDED with legacy/ still on disk. Artifacts: bin/hot.package.bin+elf (hot text 14.36KB), bin/cold.package.bin+elf (cold text 1.29MB), bin/main.cpp.o. One warning, kernel-owned: liblvgl lv_obj_style.h -Wdeprecated-enum-enum-conversion under gnu++20 (PROS template's own header, not ours). FOUND: failure now attributable — anything that breaks after deletion is deletion's fault, and nothing did.
17:40:12 START Phase 2 — THE DELETION. Authority: C6-COMPLETED §8 (unconditional; 34/34 classified; knowledge already salvaged to legacy-command-vocabulary.md / hardware-assumptions.md / diagnostics-plan.md). Recovery pointer: git show 691c656:<path> (last commit containing legacy/).
17:40:24 DONE Phase 2 deletion: git rm -r src/legacy include/legacy — 34 files staged deleted (18+16, matches C6 §4 count). Now the real test: plain 'make' with no exclusions.
17:40:33 DONE plain 'make' post-deletion: SUCCESS, identical artifacts (hot 14.36KB text / cold 1.29MB). 'make' works for the first time since the June M0 quarantine. START Phase 3 — broadening CI guards to all of include/shulib/.
17:41:57 MUTATE both broadened guards proven RED on planted violations, then restored (angle.hpp clean). sim-internal includes confirmed exempt from guard 2 (exclusion load-bearing). Now: host suite + ARM gate.
23:27:12 DONE Phase 3 verification: host suite 659/915570/3 skips (unchanged), both broadened guards green + mutation-red, ARM gate CLEAN at 102 headers, make green. START legacy-reference sweep.
23:27:39 DONE legacy sweep, code scope: zero path-shaped references (grep 'legacy/' + legacy includes = NONE across include/shulib, src, test, build files). Word-level mentions remain ONLY as design-history prose in v2 headers/tests (escapeJSONString lesson, arc-step bug) and as VEX hardware terminology in vendor pros/liblvgl headers — none reference the deleted tree. Docs sweep happens after the reorg.
23:29:06 START Phase 4 — docs reorg. Map: docs/planning/{chunks,RESUMING.md,build-order.md,transcripts} -> docs/internal/ (process, removable-as-a-unit per coordinator refinement); docs/planning/{roadmap,shulib-v2-master-plan,hardware-assumptions,diagnostics-plan,legacy-command-vocabulary}.md -> docs/ (public documentation). This log continues at docs/internal/chunks/C7-PROGRESS.md after the move.
23:29:06 DONE moves (git mv, history follows). docs/planning/ no longer exists. Now: cross-reference fixes.
23:36:05 DONE hardware-assumptions.md self-sufficiency: 9 internal-pointing contexts rewritten (brief link, build-order prose x6, two chunk-record links) — content inlined or re-attributed to 'development log, shulib-v2 branch'; Phase-R usability unchanged. Now diagnostics-plan + vocabulary doc.
23:39:48 DONE roadmap.md: 27 edits — every internal ref eliminated (chunk-record links -> 'development log, shulib-v2 branch'), you-are-here rewritten for C7 (structural clause CLOSED, on-robot clause OPEN at R3, next D1), WS11 bullet flipped [~] with deviation note + evidence, M2 DoD annotated (does not close until R3 + D2), M0 promises marked fulfilled.
23:41:13 DONE build-order.md current position (C7 section added, honest-status item 3 closed, Phase C COMPLETE) + fixed stale 'pending review' claims for the since-committed C2/C3/C4 (1206dbe/cad33bc/2a1cfc3) in both build-order and roadmap. START Phase 5 — the README.
23:45:31 DONE Phase 5 README: rewritten as the honest front door; its cpp example extracted verbatim from the fence and compiled at full suite strictness (host) AND for the V5 target; its commands executed exactly as written (cmake -S test -B build/test; cmake --build build/test -j; ./build/test/shulib_tests -> 659/915570 SUCCESS; make -> hot+cold .bin). pros upload NOT executed (needs a V5 — stated in the README itself).
23:46:58 DONE removability proof: (1) grep of README + test/README + docs/*.md for docs/internal|internal/|chunks/|RESUMING|build-order|-COMPLETED.md|-PROGRESS.md = EMPTY; (2) with docs/internal MOVED ASIDE, the 7 public md files' 23 links all resolve; restored intact. rm -rf docs/internal leaves a coherent, fully-linked documentation set.
23:47:51 DONE legacy-command-vocabulary.md post-deletion accuracy pass: title/method/§6 now state the deletion happened and route re-runs through git show 691c656. START C7-COMPLETED.md.
23:52:26 DONE C7-COMPLETED.md written (deletion inventory + recovery commit, reorg map, removability proof + interpretation, what make produces / does NOT do, 11 decisions, all six contract parts, freeze-register correction re: C6's 'C7 supplies the on-robot consumer' expectation).
23:52:26 DONE final verification battery, all green: link check 41 files / 74 links all resolve; host suite 659/915570/3 skips unchanged; PROS guard + layering guard green at broadened scope; ARM gate 102 headers CLEAN; make green (bin/hot.package.bin 14,704 B + bin/cold.package.bin 1,356,264 B).
23:52:26 DONE C7. Working tree state: 34 staged legacy deletions, docs renames (git mv), src/main.cpp + include/main.h + README + ci.yml + 15 comment-path fixes, C7-PROGRESS/C7-COMPLETED untracked. NOT committed, NOT pushed — pending review per the brief. M2 structural clause CLOSED; on-robot clause OPEN (R3). Next: D1.
