# C3 Progress Log — H-drive kinematics

_Live log; `tail -f` friendly. Vocabulary: START / DONE / MUTATE / DECIDE / BLOCKED / FOUND._

---

[20:01:02] START C3 — reading brief (C3-hdrive-kinematics.md), C1/C2/A3 COMPLETED docs, build-order Phase C, RESUMING.md, hardware-assumptions.md, then full kinematics/motion/sim/diag/control/localization code sweep before touching anything.
[20:09:48] DONE reading: brief, C1/C2/A3-COMPLETED, build-order Phase C, RESUMING, master plan §13; code: kinematics/ (all 6), move_to_pose, motion.hpp, debug_record, drive_plant, scenario, motion_test_rig.
[20:09:48] FOUND the central design tension early: brief wants an "automatic turn-then-drive fallback" AND "the motion layer must not change". Master plan §13 #5 assigns the fallback trigger to the motion/Chassis layer; C1 D12 forbids turn-then-drive planning anywhere in the engine. Working the resolution before writing any code.
[20:09:48] FOUND: TermSink already renders " SFB" from strafeFallbackActive (A1 shipped the formatter); DebugRecord.strafeFallbackActive exists with "— C3" producer tag. The population point must be the record producer = the ONE MoveToPose pipeline. Reading remaining tests + register rules next.
[20:35:07] DONE baseline verified: 527 cases / 859,931 assertions green at 7fcb3d4; tree clean.
[20:35:07] DONE captured BIT-EXACT goldens of the CURRENT forward() (pre-change) — XOR-of-bit-patterns + sums over an 8^4 wheel-speed sweep for xDrive(7), xDrive(10), the synthetic orthogonal table, and tank(12); verified optimization-invariant (-O0 == -O2). These become the strict-generalization regression pins: the pseudo-inverse must reproduce every previously-accepted table BIT-IDENTICALLY, not merely within tolerance.
[20:35:07] DECIDE the pseudo-inverse keeps TWO computation paths, selected ONCE at construction by the EXACT predicate the old precondition used (|col·col| <= 1e-9 relative): previously-accepted tables run the historical projection VERBATIM (bit-identity guaranteed by construction), newly-accepted non-orthogonal tables run the full (A^T A)^-1 A^T. Rejected: general-path-only (1-ulp drift on every existing drive would silently invalidate every recorded C1/C2 accuracy digit and weaken the no-regression claim to "approximately").
[20:37:17] DECIDE the H-drive ships as a MatrixKinematics PRESET (kinematics/h_drive.hpp: HDriveConfig + hDrive()) over a dedicated HDriveKinematics class — master plan §13 #15 LOCKED the hybrid backend ('the linear holonomic drives are one impl, MatrixKinematics') and xDrive() set the preset idiom. Rows derived from rigid-body kinematics: left [1,0,-w/2], right [1,0,+w/2], strafe [0,1,a] with a = the strafe wheel's +FORWARD coordinate (same convention as TrackingWheelSpec::Lateral). Off-centre strafe wheel => v.t = a != 0 => the non-orthogonal column the pseudo-inverse exists for.
[20:37:17] DECIDE strafe authority = strafeSpeedRatio (derivable geometry: strafe-wheel top surface speed / drive top surface speed, 1.0 for same cartridges+wheels) x strafeTractionDerate (NOT derivable without a robot: single weakly-loaded omni pushing the whole mass on foam). The derate is the guess -> PROVISIONAL (A4: HA-54), default 0.35 = master plan's locked '~0.35 sysid-measured default'. Honest reading: on the 15-inch H-bot the geometric ceiling contributes 1.0; the ENTIRE 0.35 is a registered provisional. Test stand-in geometry (11in track, strafe wheel 4in aft) -> HA-55.
[20:37:17] DECIDE conditioning guard = relative Gram determinant relDet = det(AtA)/(SumH2*SumV2*SumT2) in [0,1] (Hadamard), REJECT below 1e-6 at construction (red-on-failure, the tree's precondition discipline; 'flag' has no channel in a pure-math leaf). Bound: relDet>=1e-6 => kappa(normalized Gram) <= ~7e6 => >=~10 good digits in forward(). Physical drives sit at ~1 (X: 1.0; H-bot: 0.94). Subsumes true-rank checking (parallel columns => det=0), which the per-column check alone MISSES once orthogonality is no longer required. Host-decidable pure-numerics constant => deliberately NOT an HA entry (register rule 1; C2 kMaxStalledPaces precedent) — the brief's 'HA-nn if chosen' yields to the register's own rule 1; reasoning recorded.
[20:37:17] DECIDE the turn-then-drive fallback SHAPE: the engine's fallback is authority-limited translation with rotation left FREE — turn-WHILE-drive, emergent from C1's clamp + decoupled loops, never a sequenced decomposition. Sequencing at engine level is rejected because (1) it would violate StrafeTo's hold-heading contract, (2) it invents unrequested intermediate headings mid-field (safety + predictability), (3) it is dominated by the simultaneous version whenever it would help. strafeFallbackActive = 'the authority clamp bound meaningfully this tick' (beyond a 1%-of-maxLin legibility floor so tank/settle PID noise cannot spam SFB), populated at the producer in the ONE shared MoveToPose pipeline — the A1-reserved population point ('strafe fallback at C3', debug_record.hpp line 15). Rejected: a sink-side decorator inferring the clamp from |vy|==limit equality-riding (fragile inference vs first-hand producer knowledge); any per-drivetrain motion special case (the landmine).
[20:37:17] START implementing: matrix_kinematics.hpp pseudo-inverse + guard; h_drive.hpp preset; kinematics.hpp F5 doc clarification (D11-confirmed wording, doc-only); move_to_pose.hpp SFB population (telemetry-only).
[20:56:56] DONE matrix_kinematics.hpp generalized to (AtA)^-1 At with dual path + relDet conditioning guard; h_drive.hpp preset written; kinematics.hpp F5 doc clarified (D11 confirmed wording, doc-only); move_to_pose.hpp populates strafeFallbackActive (telemetry-only, 1%-of-maxLin legibility floor).
[20:56:56] DONE probe: H table (11in track, strafe 4in aft) round-trip + independent hand-inverse oracle agree to 3.6e-15; on-centre H exact. Bit-identity capture re-run against MODIFIED headers: IDENTICAL to pre-change goldens on xDrive(7)/xDrive(10)/synthetic/tank(12) — the strict-generalization guarantee holds by construction AND by measurement.
[20:56:56] DONE full suite re-run with all header changes, ZERO test edits: 527/859,931 green — including the old 'non-orthogonal table rejected' case, which STILL throws (its 2-wheel table is rank-deficient in R^2, det(AtA)=0, caught by the NEW conditioning guard; the stale comment gets updated with the new reason).
[20:56:56] FOUND (design-time, worth recording): once orthogonality is not required, the pre-C3 per-column rank check is NOT sufficient for invertibility — individually-nonzero but linearly-DEPENDENT columns (two parallel wheel directions) pass it while det(AtA)=0. The old code was protected only by the orthogonality precondition it is now shedding. The relDet guard exists precisely to close the hole the relaxation would otherwise open.
[20:56:56] START test suites: matrix pseudo-inverse regression pins + conditioning; h_drive geometry oracles; motion-layer H-bot suite; H routine accuracy.
[21:01:45] DONE kinematics test suites: matrix_kinematics_test.cpp +7 cases (bit-identity XOR goldens vs pre-C3 build, relaxation-is-real, normal-equation certificate on inconsistent inputs, 400-table seeded sweep, conditioning boundary both sides, scale-free guard, NaN evasion) + h_drive_test.cpp 9 cases (independent rigid-body oracle x200 random geometries, tank-subset equivalence, signatures, hand closed-form inverse, authority derivation pins, on-centre degenerate, rejections). Suite 527 -> 542 cases / 862,852 assertions, all green — bit-identity checksums MATCH inside the real CMake build.
[21:01:45] START motion-layer H-bot suite: stand-in geometry (HA-55) into motion_test_rig.hpp, then motion_hdrive_test.cpp.
[21:09:11] DONE motion_hdrive_test.cpp (10 cases): C1 primitives unmodified on the H-bot (12-seed invariant sweep, 12/12 trials entered fallback, worst clean pos err 0.237 in — MATCHES the X-drive's 0.238), beyond-authority StrafeTo (true crab peak EXACTLY 21.0 in/s = authority*maxLin, heading held 0.0 deg, 50+ SFB records, quiet exit record), TermSink end-to-end ' SFB' legibility, X-drive never-SFB pin, tank honest-timeout + visible SFB, turn-WHILE-drive A/B (aligned-heading MoveToPose 1.92 s BEATS pure crab 2.71 s — the fallback is correct, not merely bounded; flag engages by record 5, releases before the last quarter), composed hostility x3 seeds, C2 scheduler chain with id-stamped SFB. Suite 552/912,445 green.
[21:09:11] FOUND (test-authoring, recorded honestly): first version of the scheduler-chain case put the strafe leg along field -Y at heading -90 deg — which is body-FORWARD, no strafe demand, sawId3Sfb false. A geometry error in MY test, not a code defect: the fix aims the leg along field +X (= body-left at that heading). Lesson pinned in-comment: fallback tests must check the BODY-frame displacement, not the verb name.
[21:09:11] START motion_hdrive_routine_test.cpp — X and H through the SAME generator + seeds, scheduler-driven, three-way regression, side-by-side reporting.
[21:15:21] DONE motion_hdrive_routine_test.cpp (3 cases): X and H through the SAME generator+seeds, scheduler-driven. THE HEADLINE NUMBERS — clean seed 77: X n=5 finalErr 0.228175 in (reproduces the C1/C2 recorded baseline TO THE DIGIT through a fresh generator copy — an unplanned cross-check that the generator+seed pipeline is exact); H n=5 0.225 in, n=40 X 0.236 / H 0.238 in — the H-bot MATCHES X accuracy at every length, FLAT in count, paying only ~1-4%% extra time. Hostile worst: X 4.13 in (C1's exact number) vs H 4.03 in; H slopes 0.066 in/move raw / 0.029 in/s / 0.0032 in/in — same drift-carried structure as X (0.062/0.028/0.0031).
[21:15:21] FOUND (physics, worth reporting): H total hostile time is only ~1.3%% above X (351 vs 347 s over the sweep) — NOT the naive 1/0.35 penalty. The turn-while-drive migration keeps MoveToPose legs near X speed; only the explicit 1-in-7 StrafeTo legs pay the crab. Corrected my own bound-derivation comment to the measured reality (I had guessed 1.2-1.4x). H bound set 6.0 in = observed 4.03 + margin, 2x inside the ~12 in drift ceiling — same derivation discipline as C1.
[21:15:21] START register entries HA-54/HA-55 + reconciliation, then the mutation campaign.
[21:18:23] START mutation campaign — 13 planned, each break -> build -> run -> OBSERVE -> restore from cmp-verified pristine copies (never git checkout: the C3 work itself is uncommitted — the C1 process lesson). Baseline entering campaign: 556 cases / 913,561 assertions green.
[21:20:25] MUTATE M1 (general path: g01/g02 swapped — adjugate slip) -> RED, 3 cases / 3 assertions: normal-equation certificate, 400-table random sweep, boundary-accuracy case. OBSERVED SPECIFICITY: h_drive tests stay green because the H Gram has hv=ht=0 => g01=g02=0 (swapping zeros is a no-op there) — the fully-populated-Gram tests are the ONLY detectors of this slip class, which is precisely why the normal-equation table has every off-diagonal nonzero. Restored, cmp-verified, re-green 556/913,561.
[21:23:01] MUTATE M2 (fast-path predicate dead: orthogonal_=false, everything general) -> RED, 1 case / 9 assertions: ONLY the bit-identity checksums (the C2-M12 sole-detector pattern — every behavioural test tolerates ulp drift). OBSERVED SUBTLETY: the synthetic-table and tank checksums SURVIVED the mutation — their column sums are exact powers of two so the general path is bit-exact for them; only the sqrt2-based X-drive tables drift. Had the regression pin used only the synthetic table it would have been blind: the X-drive checksums are load-bearing. Restored, re-green.
[21:23:53] MUTATE M3 (conditioning guard vacuous) -> RED, 2 cases / 3 assertions: the rank-in-disguise 2-wheel case (which pre-C3 relied on the orthogonality precondition to reject) and both rejection arms of the boundary case (exact-parallel + epsilon=1e-3). The guard is load-bearing, not decorative. Restored.
[21:25:30] MUTATE M4 (hDrive strafe-row omega-coupling sign flipped: [0,1,-a]) -> RED, 3 cases / 3 assertions: rigid-body oracle, rotation signature, hand closed-form inverse. CRITICAL OBSERVATION, recorded loudly: EVERY closed-loop motion/routine test stayed GREEN — the plant and the motion share the one IKinematics object, so a geometry sign error cancels end-to-end (toWheels and forward invert each others mistake and the sim world is self-consistently wrong). The independent from-scratch oracles are the ONLY defence for preset geometry — drive_plant.hpp predicted exactly this hazard; C3 has now OBSERVED it. On a real robot this bug class surfaces only at R3s bench signature checks (HA-17/HA-55). Restored.
[21:28:28] MUTATE M5 (authority derivation drops the derate: authority = ratio = 1.0) -> RED, 10 cases / 15 assertions: every authority pin in h_drive_test AND — the valuable half — every SFB-visibility case in the motion suites (with authority 1.0 the clamp never binds, so the fallback never engages and flagged>0 REQUIREs fail: the silent-fallback-is-a-failing-test discipline detects a wrong authority NUMBER, not just a missing flag). Plus the clean X-vs-H routine (H beat X on time — impossible at real authority — caught by the h.totalTime >= x.totalTime - 1.0 sanity bound). Restored.
[21:31:07] MUTATE M6 (the SILENT fallback: strafeFallback forced false) -> RED, 6 cases / 6 assertions — every visibility pin independently: beyond-authority record audit, H invariant sweep (trialsWithFallback>=4), scheduler id-stamped SFB, the TermSink terminal line, the engage-then-clear trajectory case, tank visible-timeout. "A silent fallback is a failing test" is now a 6-way proven property, not a slogan. Restored.
[21:35:58] MUTATE M7 (the flag LIES: strafeFallback forced true) -> RED, 5 cases / 5 assertions: X-never-SFB, the tank legibility-floor case, the flaggedOffLimit audits in the H sweep + beyond-authority case, and the engage-then-clear trajectory (lastFlagged bound). A permanently-on flag is as caught as a silent one — both directions of flag honesty are pinned. Restored.
[21:39:14] MUTATE M8 (C1 authority clamp DROPPED — C1 mutation-5 rerun against the real drive) -> RED, 6 cases / 154 assertions: C1s fractional-authority pin + tank vy-audit (the original detectors, still alive) AND the C3 real-drive detectors: H sweep everExceeded audit, beyond-authority TRUE-crab physics (truth rode 60 in/s where 21 is deliverable), the trajectory case, the X-vs-H clean routine. D11s clamp is now double-guarded: synthetic contract shape (C1) + real drive physics (C3). Restored.
[21:44:05] MUTATE M9 (SFB legibility floor removed: fraction=0) -> RED, 2 cases / 2 assertions: the designed tank sub-perceptible detector AND (unplanned, informative) the X-drive never-SFB pin — with no floor, ulp-level rotation dust (|body vy| exceeding vyLimit by ~1e-15 when the norm cap rides) lights the flag on a full-authority drive. The floor is load-bearing for BOTH legibility and the structural X claim. Restored.
[21:47:08] MUTATE M10 (per-column rank check vacuous — the redundancy probe) -> GREEN, analyzed and resolved: a zero column drives det(AtA) to 0 (or relDet to 0/0=NaN, which the > comparison also rejects), so the conditioning guard SUBSUMES the rejection — behaviour-preserving redundancy, NOT a detection hole (every rejected table is still rejected; the delta is only the diagnostic messages specificity, which the suite by A1-era convention does not pin). RESPONSE: the in-header doc no longer claims the rank check is load-bearing — it now states, citing this mutation, that it is defence-in-depth kept for its actionable message. Pristine snapshot refreshed to include the doc fix.
[21:48:36] MUTATE M11 (clamp applied to vx instead of vy — the axis mixup) -> RED, 8 cases / 156 assertions across BOTH drivetrain families: C1 fractional/tank audits (tank forward speed strangled to 0, vy unclamped) + all four H/tank C3 visibility-and-physics cases. Restored.
[21:49:19] MUTATE M12 (forward() = pre-C3 orthogonal projection for EVERY table — "was the pseudo-inverse necessary?") -> RED, 9 cases / 344 assertions: all 5 pseudo-inverse kinematics cases + hDrive hand-inverse + derate-0 round-trip AND two PHYSICS cases — H TurnTo no longer rotates in place (strafe-wheel roll misattributed to phantom translation in the plants truth) and the beyond-authority crab physics break. The chunk is provably necessary: shipping the H-drive on the old projection would have produced a visibly wrong sim world, not a subtle bias. Restored.
[21:50:38] MUTATE M13 (TermSink " SFB" rendering dropped) -> RED, 2 cases / 2 assertions: the C3 end-to-end terminal legibility case AND A1s own flags-iff-set golden (which set strafeFallbackActive at A1, waiting for this producer). Campaign CLOSED: 13 mutations, 12 RED / 1 explained-GREEN (M10, behaviour-preserving redundancy, doc corrected). All 4 mutated headers cmp-identical to pristine; zero mutation markers (grep); suite re-green 556/913,561.
[22:04:30] DONE documentation contract: roadmap checkbox [x] with evidence + 'you are here' moved to post-C3/next-C4; build-order Current position rewritten (C3 block + post-C3 verified numbers); hardware-assumptions HA-54/HA-55 + status counts, reconciliation grep clean both directions; C3-COMPLETED.md written at 654 lines / 13 sections including 'What we now know for certain, and what we do not' and the F6 amendments (fallback-surface NO / authority-passthrough YES / turn-while-drive as API docs / the shared wheel-budget additive path / drivetrain-as-config).
[22:04:30] DONE final verification, all observed: suite 556 cases / 913,561 assertions green under strict -Werror (3 deliberate skips unchanged, wall ~1.2 s); GUARD 1 PASS (PROS-free incl. h_drive); GUARD 2 PASS (core sim-free); ARM cross-compile CLEAN, 87 headers (h_drive.hpp picked up by the generated list). NOTHING COMMITTED — working tree left for review: 8 modified + 6 new files.
[22:04:30] DONE C3 complete pending independent verification. Headline: the same auton runs the H-bot — measured, not asserted. X n=40 clean 0.236 in / H 0.238 in, flat in count; hostile X 4.13 / H 4.03 in; H pays ~1-4%% time. D11 CONFIRMED. 13 mutations: 12 red, 1 analyzed green (M10 redundancy -> doc corrected). The chunk's two most valuable findings: geometry sign errors are closed-loop-invisible (M4 — the oracles are the only defence) and the pseudo-inverse is provably necessary (M12 — the old math produces a visibly wrong H world).
