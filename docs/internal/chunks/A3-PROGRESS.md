# A3 Progress Log — Hostile Fakes

Live log. Vocabulary: `START` / `DONE` / `MUTATE` / `DECIDE` / `BLOCKED` / `FOUND`.

---

- 00:26:10 START A3 session. Tree clean at 165555f on shulib-v2. First action: this file. Next: full required reading (brief, A2/A1 completion records, build-order, RESUMING, all sim/diag/hal-fake/localization/control headers) before any code.
- 00:27:31 DONE Required reading pass 1: brief, A1/A2 completion records, build-order, RESUMING, all sim/ headers, diag/fault+finite_guard+loop_monitor+debug_record, localizer/pilons/tracking_wheel/complementary_fusion/arc_step/correction, fake imu/gps/motor/battery/rotation/clock, pid/feedforward, telemetry seam, sim tests (scenario + odometry-truth), accuracy stubs. Now reading control/ remainder + units/math + CI + CMake.
- 00:38:10 DONE Reading complete (incl. control/, interfaces, remaining fakes, both CI guards, CMake, all sim_* + localization tests). Key facts absorbed: hooks receive (truth, now, rng); effectiveVoltage sees pre-advance t, synthesizeSensors sees t+dt; plant ctor calls synthesizeSensors at t=0; FakeImu heading is math::Angle (can never be NaN) but FakeRotation position is AngleDim (CAN carry PROS_ERR_F = +inf) — that is the HAL-contract-breach attack lane.
- 00:38:20 DECIDE Structure: per-family hostile model classes (Imu/Gps/Encoder/Power/Slip/Latency, each final : DegradationModel) + ChainedDegradation + FullHostility (owns one of each, documented order) over ONE monolithic config-flag model — because independent injectability = use one model alone, composability = the chain, and reduction of a composed failure = drop one model from the chain. Rejected: monolithic class (all pathologies tangled in one file, reduction only via config spelunking); per-sensor callback slots (re-litigates A2 D5).
- 00:38:28 DECIDE Default-hostile philosophy: CONTINUOUS pathologies (drift, noise, quantization, decimation, sag, accel-slip, latency, calibration window) are ON by default at provisional-realistic magnitudes; EVENT pathologies (dropout, sentinel breach, bad-fix window, brownout collapse, tracking bump) default OFF and are enabled per scenario. Rationale: a default-constructed hostile model that does nothing is a dead seam waiting to ship; but "everything hostile" must still complete a bounded-error run, which permanent dropouts would make vacuous. Both composed tiers get tested (realistic composed + catastrophic composed).
- 00:38:36 DECIDE Fault-raising home: new diag::HealthMonitor (mirrors LoopMonitor: reads per-tick OBSERVABLES as raw values, raises edge-triggered faults through A1's FaultLatch). Rejected: threading FaultLatch& through Localizer/PilonsOdometry (couples localization->diag mutation paths, duplicates latch authority; pilons_odometry.hpp already states "recovery policy beyond this is the fusion layer's", and fault.hpp says OdoStuck is raised by the C/E layers — the monitor is the C1-reusable glue that keeps diag a leaf).
- 00:38:44 DECIDE Append FaultCode::MotorOverTemp = 9 (append-only is the documented evolution path; F9 not frozen until H1) — thermal droop is in-scope pathology and needs a code; motor.hpp already promises a "thermal fault (§8, §18.4)". fault_test numeric pins get a new pin, none change.
- 00:38:50 DECIDE Rng grows nextGaussian() (Box-Muller, exactly 2 u64 draws per call, u1 in (0,1] so log never sees 0) — additive to A2's pinned mappings, sensor noise is Gaussian-shaped, and pinning our own mapping preserves the cross-stdlib determinism argument that justified SplitMix64. Rejected: uniform "noise" (wrong shape for sigma-based reasoning), <random> normal_distribution (implementation-defined stream — the exact reason A2 rejected <random>).
- 00:38:56 DECIDE GPS pathologies are exercised through a TEST-LOCAL naive corrector (reads FakeGps, proposes fixes) attacking the REAL Localizer+ComplementaryFusion screening — E2's GpsCorrector stays out of scope (brief: correctors -> Phase E). The test corrector is a harness probe like A2's TrapSink, not a shipped estimator feature.
- 00:39:04 START Building bottom-up: (1) Rng::nextGaussian + pins, (2) FaultCode append + HealthMonitor + unit tests, (3) hostile models family-by-family with liveness tests run as they land, (4) survival attacks (expect FOUNDs here), (5) composed + determinism, (6) M2 acceptance measurement, (7) mutations, (8) ARM/guards/docs.
- 00:42:16 DONE Baseline verified before any edit: 349 cases / 547,443 assertions green (matches A2's record exactly).
- 00:44:50 DONE Rng::nextGaussian landed (sim/rng.hpp, additive) + pins in sim_scenario_test.cpp: draw-count alignment, same-seed identity, 100k-draw finiteness, mean/var moments. Suite 350/647,546 green.
- 00:48:56 DONE FaultCode::MotorOverTemp appended (=9, wire pins extended in fault_test.cpp) + diag::HealthMonitor landed (health_monitor.hpp: edge-triggered episodes, boot-window-is-not-a-loss rule, brownout hysteresis + run-latched marker, observables-not-references so diag stays a leaf) + 10 unit cases in health_monitor_test.cpp. Suite 360/647,589 green. Next: the hostile models, IMU family first.
- 01:01:32 DONE All six hostile model headers + composition landed: sim/hostile/{imu,gps,encoder,power,slip,latency}_hostility.hpp + composed.hpp (ChainedDegradation folds left-to-right; FullHostility chains power->slip->imu->gps->encoders->latency; JitterSchedule owns a private Rng). Strict-flag TU compile clean. Every invented magnitude carries a PROVISIONAL (A4) tag in-header. Now: per-family liveness/unit tests, IMU first.
- 01:05:42 DONE sim_hostile_imu_test.cpp (8 cases): window/drift/noise/dropout/consistency/identity/determinism all pinned. MEASURED: 60s drift worst over 8 boots = 0.779 deg (worst |bias| drawn 0.773 deg/min of the ±1 deg/min provisional bound) — the F2 margin question is now a number. Suite 368/648,974 green.
- 01:09:21 DONE sim_hostile_gps_test.cpp (9 cases) + sim_hostile_encoder_test.cpp (7 cases) green. Notable pins: quantization TELESCOPES (10k-tick cumulative delta error <= one tick step), odometry-under-quantization worst error < 10 tick-steps (~1.75e-3 in) over an 8s script, sentinel breach emits +inf for exactly its window. One test-side expectation fixed (a never-read channel latches the first value it sees on freeze — documented model semantics, not a defect).
- 01:15:51 DONE sim_hostile_power_test.cpp (8 cases) green. FOUND (modeling insight, not a defect): under true-voltage-control semantics, battery sag only affects motion when demand exceeds the sagged pack CEILING — a 6V cruise on an 11.6V pack is untouched, matching V5 firmware behaviour; the liveness test therefore saturates deliberately. Also fixed two test-config mistakes of mine (aggressive sag tripping the default 10.5V cutoff inside the ceiling test; a thermal bench config whose equilibrium sat 1.2C BELOW the throttle step — recomputed to cross both steps).
- 01:19:35 DONE sim_hostile_slip_test.cpp (7 cases) green. The required signature holds end-to-end with EXACT accounting: 2s window at retain 0.6 -> truth 64.0 in while drive encoders imply 80 in of wheel travel (overcount, predicted direction), tracking odometry error < 1e-6 through it (the D9 architecture proof), and a Pid closed loop converges THROUGH a slip window. Two test-side lessons: accel-slip needs one baseline tick of spin history, and slip-window edges must sit off the fp tick grid.
- 01:22:48 FOUND (model boundary): with latency an exact multiple of dt, the ring cutoff sat on an fp knife-edge — the served delay flipped between k and k+1 ticks on ~1e-17 arithmetic dust. Fixed IN THE MODEL with a 1 ns inclusivity grace (documented in latency_hostility.hpp); the exact-delay pins now hold to 1e-12.
- 01:22:48 FOUND (real physics of the stack): when motion STOPS under IMU latency, the stale-heading drain keeps advancing the reported heading while the wheels truthfully report zero travel — the odometry offset correction then injects a phantom micro-translation bounded by omega*L*|offset| (~0.14 in at 1.5 rad/s, 20 ms, 4.5 in offset). Latency converts a clean stop into one final bounded lie, then the error freezes. Pinned in sim_hostile_latency_test.cpp; this is exactly the class of surprise E2's latency compensation must handle.
- 01:22:48 DONE sim_hostile_latency_test.cpp (7 cases): exact per-channel delay, startup staleness, zero-latency identity, omega*L heading trail measured through the plant (-0.030 rad at 1.5 rad/s / 20 ms), bounded-and-frozen odometry corruption, GPS delay. Full suite green: [doctest] test cases: 406 | 406 passed | 0 failed | 4 skipped
- 01:28:29 FOUND (DEFECT, Localizer/WS5): BOOT-WINDOW POISONING, observed. A robot sitting perfectly STILL through a 2 s IMU calibration window ends with its fused pose 10.82 inches from where it started — calibration garbage heading swings flow through the odometry offset correction (dTheta*offset = phantom translation up to ~14 in/tick) and the Localizer folds those deltas in even while it reports quality 0/Uninitialized. The protective FLAG was honest; the POSE was not. With no correctors at M2 the poison is permanent. Fix belongs to the Localizer (pilons_odometry.hpp explicitly assigns recovery policy to the fusion layer): do not fold odometry deltas or proposals before the IMU has EVER been ready.
- 01:28:29 FOUND (DEFECT, Localizer/WS5): MID-RUN IMU LOSS MISREPORTED, observed. After a 3 s healthy run, a dropout makes qualityClass() report Uninitialized (0) instead of Degraded (3). A skills gate reading Uninitialized would treat a robot that HAD a good estimate and lost its heading authority like one still booting — the wrong recovery in both directions. Fix: distinguish never-ready (Uninitialized) from was-ready-lost (Degraded), quality 0 in both.
- 01:28:29 START Fixing both in include/shulib/localization/localizer.hpp (build-order rule 4: fix in the owning chunk), plus header design notes; test-loop monitor wiring gets the C1 shape (odo-health observation gated on IMU readiness during boot).
- 01:41:47 DONE Localizer fixed in its owning chunk (localizer.hpp): (1) boot guard — never-ready ticks fold no odom deltas and no proposals; (2) the garbage->ready TRANSITION tick also folds zero (its delta straddles the boundary — observed ~2.93 in of residual phantom from that single tick before this second cut); (3) mid-run loss now reports Degraded (was Uninitialized), scalar 0 in both. everUpdated_ distinguishes a real transition from a ready-from-construction first update, so existing first-tick semantics are untouched. Full suite 416/677,588 green incl. all 10 survival attacks AND every pre-existing localizer/odometry test.
- 01:41:47 DONE Survival matrix green: boot garbage (fused drift 10.82in -> <0.05in), IMU dropout (IMU_LOST + Degraded), +inf sentinel (ODO_STUCK, ~1in bounded frozen loss, no NaN ever), frozen encoder (estimator-blind — honest gap — caught by the C1-shape cross-check), GPS off-strip (BIT-equal to dead-reckon), out-of-gate lie (GPS_GATE_REJECT, pose unmoved <0.01in), in-gate lie (bounded <8.5in, heals to <0.5in), brownout (BROWNOUT latched, motors dead, run completes), jitter (LOOP_OVERRUN == spike count exactly, Pid still converges), thermal (MOTOR_OVER_TEMP, speed halved, not dead). Next: owner-side regression pins in localizer_test.cpp.
- 01:49:21 FOUND (DEFECT, Localizer/WS5 — the composed model caught what the single-family attack missed): READY OUTRUNS THE DATA PATH. With IMU latency composed onto the calibration window, isReady() flips true at t=2.00 but the heading STREAM is still serving delayed garbage — the first post-transition fold differenced against a garbage prevHeading and leaked 3.65 in into the fused pose (probe: fusedErr jumps 0 -> (+2.95,-2.15) at exactly t=2.01, flat thereafter). The one-tick transition guard was necessary but not sufficient: the boundary sits in the STREAM, delayed by an unknown data-path latency, not at the flag. This is precisely the reducibility argument working — the composed failure was attributed by probing, and no individual-family test could have seen it (window alone: no delay; latency alone: no garbage).
- 01:49:21 DECIDE Fix: a BOOT SETTLE WINDOW in the Localizer (bootSettleTime, default 0.1 s) that holds the fold after the first ready tick — applied ONLY when a not-ready phase was actually witnessed (sawNotReady), so a ready-from-construction boot keeps exact current behavior and no existing test changes meaning. Rejected: quarantining folds on lastDeltaImplausible (a ±90-or-less garbage boundary draw evades the flag ~50% of the time, and it would contradict the documented M2 flagged-but-still-integrated semantics mid-run); making the model delay ready too (hides a REAL hardware hazard the stack must survive — status flags and data paths genuinely skew). Consequence made explicit: motion commanded before quality leaves Uninitialized is unaccounted — the C1 contract is wait-for-live-estimate, which the tests now model.
- 01:55:18 START Resumed after an accidental stop (coordinator confirmed nothing needs undoing). Suite is red exactly mid-fix: finishing the settle-window contract updates in localizer_test + survival + composed tests, then acceptance/mutations/docs.
- 02:05:00 DONE Settle-window contract landed end to end. Suite 428/681,056 ALL GREEN. Evidence the fix is the root-cause fix: the composed 14 s run's worst fused error fell 3.81 in -> 0.261 in (now matching the drift/noise/latency error budget); the normal ready-from-construction path is pinned unchanged (new localizer_test case). Test-meaning changes, logged per the rules: (1) localizer transition-tick case now asserts the WIDER settle-window hold (strictly stronger contract, nothing weakened); (2) survival boot phase 2 and the composed script now hold still until qualityClass leaves Uninitialized — modeling the C1 wait-for-live-estimate contract the fix imposes (the alternative, folding while blind, is the defect itself); (3) the composed determinism case became CLOSED-loop so a different seed diverges the PHYSICS — under an open-loop script the seed only perturbs the sensor lies (truth-side hostility is deliberately deterministic; recorded as an honest limitation); (4) imu ablation sweeps seeds 1/3/10 spanning bias fractions +0.13/-0.77/-0.93 of the bound (representative range, documented in-test, not picked to pass).
- 02:07:53 DONE The M2 acceptance stub is UNSKIPPED and real. Measured across 10 seeded boots (seeds 1..10, spanning drift draws +0.13 to -0.93 deg/min of the provisional ±1 deg/min bound), full composed hostility, 60 s of straight legs after a 2.5 s boot hold: worst END-of-run heading error 0.912 deg — PASSES the frozen 1.0 deg cap — but worst INSTANTANEOUS error 1.065 deg (a noise spike on the near-bound boot crossed the cap mid-run). The margin at the pessimistic drift bound is ~zero BY CONSTRUCTION (1 deg/min x 60 s = 1 deg): the stack adds no error of its own, the sensor IS the budget. R4's measured drift decides the real margin; Phase E heading correction (E3) is what buys any. F2 untouched.
- 02:07:53 START Mutation battery (7 planned): fault-raise removal, drift no-op, static-Rng determinism break, finite-guard defeat, boot-guard defeat, edge-trigger break, chain skip. Each: break -> build -> run -> observe red -> restore -> re-green.
- 02:09:18 MUTATE #1 HealthMonitor drops the Brownout raise -> RED: 5 cases / 11 assertions (brownout+cascade+reset units, survival brownout, composed catastrophic); 424/429. Restored.
- 02:09:50 MUTATE #2 ImuHostileModel drift term made a no-op -> RED: 3 cases / 5 assertions (the exact-drift pin, the 60s drift-number liveness floor, the composed imu ablation's worst-of-boots signature); 426/429. A silently dead drift pathology cannot ship. Restored.
- 02:11:15 MUTATE #3 IMU noise drawn from a shared static Rng instead of the plant's seeded stream -> RED: 2 cases / 302 assertions (composed same-seed replay: BOTH the truth memcmp and the fused-trace memcmp; the imu same-seed determinism sweep); 427/429. Note the closed-loop determinism case is what makes the TRUTH memcmp catch this — the leaked stream reaches the physics through the controller. Restored.
- 02:11:52 MUTATE #4 PilonsOdometry finite guard defeated (finite := true) -> RED: 3 cases / 7 assertions — the +inf sentinel reached the POSE and the finiteness REQUIREs aborted both the survival sentinel case and the composed catastrophic case (FATAL), plus all 5 pilons non-finite-freeze pins; 426/429. The no-NaN-in-the-pose guarantee is enforced, not asserted. Restored.
- 02:12:19 MUTATE #5 Localizer boot guard defeated (foldDeltas := true) -> RED: 5 cases / 214 assertions — all three new localizer boot pins, the survival boot attack (fused drift + per-tick Uninitialized sweep = the 200-tick assertion storm), and the composed 14 s bounded-error case (worst error blew past 2.0 in); 424/429. The A3 flagship fix cannot silently regress. Restored.
- 02:12:42 MUTATE #6 HealthMonitor ImuLost episode gate removed (raises every tick) -> RED: 2 cases / 3 assertions (faultCount 1 vs 50 in the per-episode unit, plus the cascade-count case); 427/429. The anti-spam contract is load-bearing. Restored.
- 02:12:58 MUTATE #7 ChainedDegradation skips the imuHeading fold (a dead composed seam) -> RED: 4 cases / 5 assertions (composed liveness heading!=truth, the 14 s run's hostility-is-live floor, same-seed replay divergence checks, imu ablation signature); 425/429. A dead seam inside the composed model cannot ship. Restored.
- 02:17:00 DONE Verification battery: suite 429 cases / 681,086 assertions green (3 skipped: two M3 stubs + the R3 gps field-cal oracle — the M2 stub is now LIVE); CI GUARD 1 (PROS-free incl. sim/hostile + diag/health_monitor) PASS; CI GUARD 2 (core never includes sim/) PASS; ARM cross-compile of ALL 77 v2 headers under the strict flag set CLEAN. No mutation remnants (grep verified; the one MUTATION hit is A2's pre-existing doc comment in truth_integrator.hpp).
- 02:17:00 START Documentation contract: roadmap checkbox + you-are-here, build-order position, Freeze Register note, A3-COMPLETED.md.
- 02:24:09 DONE Documentation contract discharged: roadmap.md (A3 blurb + A3 task entry with cited evidence; the M2 acceptance carried as [~] with the honest caveat; "you are here" -> A3 done / A4 next; status-verified line updated to 429/681,086 + 77 ARM headers), build-order.md (current position + "made HOSTILE at A3" residual), A3-COMPLETED.md written (flaws-found as its own prominent section, 12 decisions, 25 provisional magnitudes queued for A4, 7 observed mutations, full DoD).
- 02:26:33 DONE A3 complete. Final: 429 cases / 681,086 assertions green (3 deliberate skips), both CI guards pass, 77/77 headers ARM-clean, 7/7 mutations observed red and restored, nothing committed (tree left for review at 165555f). Hostility found what it was built to find: three Localizer defects fixed at the source, one detection hole recorded with named owners, two physics findings pinned as tests, the M2 acceptance measured at 0.912 deg worst-of-10-boots against the frozen 1.0 deg cap with the zero-margin caveat recorded. A4 next.
